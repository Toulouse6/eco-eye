specVersion: "v1beta"
serviceId: "eco-eye-back"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "eco-eye-back-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
